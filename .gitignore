# Claude Code local config (not project source)
.claude/

# macOS
.DS_Store

# NOTE: index.html IS tracked — it is the deployed entry point (Vercel serves it
# at the root URL). Weekly, replace it with the data-baked version from
# data-injector.html, then commit + push to auto-deploy.

# Local data files (keep raw sales data out of the repo)
*.xlsx
*.xls
*.csv
